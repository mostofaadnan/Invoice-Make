<table class="table table-striped table-bordered table-sm data-table" id="invoicetable">
    <thead>
        <tr>
            <th> #@lang('home.sl') </th>
            <th> @lang('home.item') @lang('home.code') </th>
            <th> @lang('home.description')</th>
            <th> @lang('home.mrp')</th>
            <th> @lang('home.discount') @lang('home.price')</th>
            <th> @lang('home.quantity') </th>
            <th> @lang('home.action')</th>
        </tr>
    </thead>
    <tbody id="datatablebody">
    </tbody>
</table>